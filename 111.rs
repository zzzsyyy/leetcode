#[test]
fn rotate(matrix: &mut Vec<Vec<i32>>){
    let len = matrix.len();
     
}


fn main() {
    let mut matrix = [
        [5, 1, 9, 11].to_vec(),
        [2, 4, 8, 10].to_vec(),
        [13, 3, 6, 7].to_vec(),
        [15, 14, 12, 16].to_vec(),
    ]
    .to_vec();
    let len = matrix.len();
    println!("{:?}", len);
}
